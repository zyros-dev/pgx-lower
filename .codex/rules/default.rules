# Generated by `pgx-cli codex-policy rules`.
# match: ssh comfy true
# match: docker ps
# match: psql -c 'SELECT 1'
# Opaque shell wrappers and raw cat/tail are blocked below because
# project rules cannot match arbitrary shell-body or path substrings.
# Use typed pgx-cli commands, direct local inspection commands, or
# `pgx-cli codex-policy check -- <cmd...>` for detailed classifier output.

# ## Agent Efficiency Rules
#
# - Use `pgx-cli` for high-risk commands: thor, Docker, PostgreSQL, CTest, CMake,
#   Ninja, task-spooler, Mutagen, benchmarks, and migrated helper scripts.
# - Do not raw-cat large logs or IR dumps. Use `pgx-cli logs ...` or
#   `pgx-cli ir inspect ...`.
# - After a full gate failure, run the focused reproducer before rerunning the
#   full gate.
# - Before claiming PR-ready, run the evidence/readiness checks required by the
#   active spec.
# - Edit local files and run compute on thor. Do not edit source directly in the
#   thor checkout.
# - Treat generated and ignored artifacts as execution state that must be fresh.

prefix_rule(
    pattern = ["ssh"],
    decision = "forbidden",
    justification = "Use pgx-cli run thor -- ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["docker"],
    decision = "forbidden",
    justification = "Use pgx-cli run docker -- ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["psql"],
    decision = "forbidden",
    justification = "Use pgx-cli run psql ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["pg_regress"],
    decision = "forbidden",
    justification = "Use pgx-cli dev test ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["ctest"],
    decision = "forbidden",
    justification = "Use pgx-cli dev test ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["cmake"],
    decision = "forbidden",
    justification = "Use pgx-cli dev build ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["ninja"],
    decision = "forbidden",
    justification = "Use pgx-cli dev build ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["tsp"],
    decision = "forbidden",
    justification = "Use pgx-cli queue ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["mutagen"],
    decision = "forbidden",
    justification = "Use pgx-cli sync ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["just"],
    decision = "forbidden",
    justification = "Use pgx-cli ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["bash", "-lc"],
    decision = "forbidden",
    justification = "Use pgx-cli typed commands or non-opaque local inspection so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["bash", "-c"],
    decision = "forbidden",
    justification = "Use pgx-cli typed commands or non-opaque local inspection so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["sh", "-c"],
    decision = "forbidden",
    justification = "Use pgx-cli typed commands or non-opaque local inspection so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["zsh", "-lc"],
    decision = "forbidden",
    justification = "Use pgx-cli typed commands or non-opaque local inspection so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["zsh", "-c"],
    decision = "forbidden",
    justification = "Use pgx-cli typed commands or non-opaque local inspection so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["cat"],
    decision = "forbidden",
    justification = "Use pgx-cli logs ... / pgx-cli ir inspect ... / sed -n for small source reads so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["tail"],
    decision = "forbidden",
    justification = "Use pgx-cli logs ... / pgx-cli ir inspect ... so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["gh", "pr", "comment", "1", "--body", "`pgx-cli dev gate review`"],
    decision = "forbidden",
    justification = "Use gh pr comment --body-file so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["benchmark/tpch/aggregate.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["./benchmark/tpch/aggregate.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["benchmark/tpch/fxt_to_flamegraph.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["./benchmark/tpch/fxt_to_flamegraph.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["benchmark/tpch/metrics_collector.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["./benchmark/tpch/metrics_collector.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["benchmark/tpch/run.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)

prefix_rule(
    pattern = ["./benchmark/tpch/run.py"],
    decision = "forbidden",
    justification = "Use pgx-cli <typed workflow command> so Mutagen, transcripts, and output limits are enforced.",
)
